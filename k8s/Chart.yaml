apiVersion: v2
name: xib
description: Portable Kubernetes distribution of the In-a-Box security stack
type: application
version: 0.2.0
appVersion: "0.2.0"

dependencies:
  - name: sib-k8s
    alias: sib
    version: "0.1.0"
    repository: "file://charts/sib-k8s"
    condition: sib.enabled

